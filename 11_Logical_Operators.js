let x = 5
let y = 6
let a = 8
let b = 9

//Part of math And(&),Or(|),Not(!) gate

// let result = x < y && a < b;
let result = x < y || a > b;
//If you gives ! before variable then opposite value will come
console.log(!result);